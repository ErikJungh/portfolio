import { AnimateDivDirective } from './animate-div.directive';

describe('AnimateDivDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateDivDirective();
    expect(directive).toBeTruthy();
  });
});
